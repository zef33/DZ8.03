# Домашнее задание к занятию «Базы данных, их типы» - `Иванов Федор`


### Задание 1 СУБД
Кейс
Крупная строительная компания, которая также занимается проектированием и девелопментом, решила создать правильную архитектуру для работы с данными. Ниже представлены задачи, которые необходимо решить для каждой предметной области.

Какие типы СУБД, на ваш взгляд, лучше всего подойдут для решения этих задач и почему?

1.1. Бюджетирование проектов с дальнейшим формированием финансовых аналитических отчётов и прогнозирования рисков. СУБД должна гарантировать целостность и чёткую структуру данных.

Предлагаю применить реляционную СУБД. Бюджетирование — классическая задача с жёсткой структурой, транзакционностью и строгими связями между сущностями (проект → статья бюджета → транзакция → отчёт). Реляционная модель гарантирует ACID-транзакции, целостность через внешние ключи и constraints, а SQL отлично подходит для аналитических запросов с агрегациями, оконными функциями и CTE. 

1.2. Под каждый девелоперский проект создаётся отдельный лендинг, и все данные по лидам стекаются в CRM к маркетологам и менеджерам по продажам. Какой тип СУБД лучше использовать для лендингов и для CRM? СУБД должны быть гибкими и быстрыми.

Лендинги — MongoDB или Couchbase. Под каждый девелоперский проект создаётся отдельный лендинг, и структура собираемых данных может отличаться: один проект собирает имя + телефон, другой — ещё этаж и количество комнат, третий — заявку на ипотеку с расширенной анкетой. Документоориентированная СУБД позволяет хранить разные схемы в одной коллекции — не нужно каждый раз менять структуру таблиц. Данные лидов — это по сути JSON-документы, и MongoDB с ними работает нативно и быстро.

CRM — два жизнеспособных варианта:
MongoDB / Couchbase — если приоритет на гибкость (кастомные поля под каждый проект, разные воронки продаж) и скорость. Лиды из лендингов сразу льются в CRM в едином формате документов — без ETL-преобразований.
PostgreSQL (с JSONB-колонками) — если нужен компромисс между гибкостью и транзакционностью. JSONB даёт документную гибкость, но при этом остаются ACID-гарантии и SQL-аналитика по продажам. Многие современные CRM именно так и устроены.

1.3. Отдел контроля качества решил создать базу по корпоративным нормам и правилам, обучающему материалу и так далее, сформированную согласно структуре компании. СУБД должна иметь простую и понятную структуру.

Реляционная СУБД (PostgreSQL) — если нормы и правила укладываются в чёткие категории и подкатегории с предсказуемой структурой: раздел → подраздел → документ → версия. Схема «таблица с self-reference (parent_id)» даёт простую и прозрачную иерархию, а SQL-запросы через WITH RECURSIVE легко обходят дерево. Плюс — версионность документов, права доступа по разделам, ссылки на связанные документы — всё это естественно ложится в реляционную модель.

1.4. Департамент логистики нуждается в решении задач по быстрому формированию маршрутов доставки материалов по объектам и распределению курьеров по маршрутам с доставкой документов. СУБД должна уметь быстро работать со связями.

Можно применить графовую СУБД. Задача логистики — это по сути граф: объекты — вершины, дороги между ними — рёбра с весами (расстояние, время, пропускная способность). Графовая СУБД хранит связи как сущности первого класса — запрос вида «найти кратчайший путь от склада до объекта через три промежуточные точки» в Neo4j пишется одной строкой на Cypher, а в реляционной базе потребует нескольких JOIN-ов или рекурсивных запросов, которые на больших графах работают медленно.
Распределение курьеров — это задача о назначениях (assignment problem), и графовая модель естественно представляет её: курьёр → маршрут → точки доставки → объект.

---

### Задание 2 Транзакции
Пользователь пополняет баланс счёта телефона, распишите пошагово, какие действия должны произойти для того, чтобы транзакция завершилась успешно. Ориентируйтесь на шесть действий.

Вот 6 шагов, которые обычно происходят при пополнении баланса счёта телефона:

1. Пользователь инициирует платёж. Он выбирает способ пополнения (в мобильном приложении оператора, на сайте платёжного сервиса, через банкомат или терминал) и вводит данные: номер телефона, сумму платежа и платёжные реквизиты (например, данные банковской карты). Система проверяет корректность введённых данных — например, формат номера и допустимый диапазон суммы.

2. Система формирует запрос на авторизацию платежа. Данные передаются в платёжный шлюз, который связывается с банком-эмитентом карты для подтверждения операции. В случае онлайн-платежа может потребоваться дополнительная аутентификация пользователя (например, ввод одноразового кода из SMS или push‑уведомления по технологии 3‑D Secure).

3. Банк подтверждает платёж. Если на счёте достаточно средств и нет ограничений, банк одобряет транзакцию и списывает сумму со счёта пользователя. Платёжный шлюз получает от банка статус авторизации и передаёт его в систему оператора связи.

4. Оператор фиксирует платёж в своей биллинговой системе. Поступает уведомление о проведённом платеже, создаётся внутренняя запись транзакции с привязкой к номеру телефона. Система проверяет, что номер активен и принадлежит действующему абоненту; при необходимости применяются правила обработки платежей (например, учёт комиссий или специальных тарифов).

5. Средства зачисляются на баланс абонента. Биллинговая система обновляет остаток на лицевом счёте пользователя, фиксирует время и сумму пополнения. В этот момент баланс становится доступным для оплаты услуг связи (звонков, интернета и т. д.).

6. Пользователь получает подтверждение операции. Ему приходит уведомление — SMS, push‑сообщение или письмо на электронную почту — с информацией о сумме, времени платежа и новом балансе. В интерфейсе приложения или на сайте также отображается актуальный остаток, а в истории операций фиксируется запись о пополнении.

---

### Задание 3 SQL vs NoSQL
Напишите пять преимуществ SQL-систем по отношению к NoSQL.

1. SQL‑системы обеспечивают свойства ACID (атомарность, согласованность, изолированность, долговечность) — это критично для финансовых операций, учёта остатков, резервирований и других сценариев, где нельзя допустить рассогласованность.
2. В реляционных СУБД структура таблиц и типы данных задаются заранее, а ограничения (NOT NULL, UNIQUE, FOREIGN KEY, CHECK) жёстко контролируются на уровне базы. Это снижает риск ошибок из‑за некорректных данных, упрощает поддержку кода и делает схему понятной для всей команды.
3. SQL — де‑факто стандарт для работы с данными: он декларативный, хорошо изучен, поддерживается инструментами аналитики, BI‑системами и ETL‑пайплайнами.
4. Благодаря SQL и строгой схеме реляционные базы легко интегрируются с BI‑инструментами (Power BI, Tableau, Superset и др.), а также с системами отчётности и хранилищами данных. Многие инструменты предполагают наличие реляционной модели и удобных метаданных (списки таблиц, колонок, ограничений). Для NoSQL часто требуется дополнительная подготовка данных (ETL/ELT) и промежуточные слои, чтобы сделать их удобными для аналитики.
5. Надёжные механизмы обеспечения ссылочной целостности и защиты данных. Внешние ключи (foreign keys) позволяют гарантировать, что ссылки на другие сущности всегда валидны, а каскадные операции (ON DELETE/UPDATE) помогают поддерживать согласованность при изменениях. Плюс в SQL‑системах хорошо развиты средства контроля доступа (роли, права на таблицы/колонки/строки), аудит и управление привилегиями — это важно для соблюдения регуляторных требований. В NoSQL ссылочная целостность чаще всего реализуется на уровне приложения, а механизмы контроля доступа могут быть менее детализированными.

---

### Задание 4 Кластеры
Необходимо производить большое количество вычислений при работе с огромным количеством данных, под эту задачу выделено 1000 машин.
На основе какого критерия будете выбирать тип СУБД и какая модель распределённых вычислений здесь справится лучше всего и почему?

В такой задаче я бы в первую очередь смотрел на характер нагрузки и тип данных. Именно этот критерий станет главным фильтром при выборе между SQL и NoSQL, а следом нужно подобрать модель распределённых вычислений, которая раскроет потенциал выбранной СУБД на кластере из 1000 машин.
В данной модели применим SQL-СУБД. Суть в том, что запрос разбивается на части, которые параллельно выполняются на разных узлах кластера, а затем результаты агрегируются. Примеры: Citus (расширение для PostgreSQL), Postgres-XL. Это позволяет эффективно использовать кластер для сложных аналитических запросов.
